{
  "name": "bfc-proxy",
  "version": "1.0.0",
  "description": "BFC Cloud proxy — forwards app requests to Claude with a server-side key.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
